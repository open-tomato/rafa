- The plan's "Structured task report" stage deletes
  `.claude/skills/progress-hygiene`; docs must not treat it as permanent.
- The sandbox blocks `rm -r`/`rm -rf` everywhere, /tmp too: empty a dir by
  `mv`-ing its contents out; `cp -R src/. dest/` copies into an existing
  one.
- Relative imports under `src/` use `.js` specifiers for `.ts` files;
  keep them when moving files.
- Tests use `bun:test`'s `spyOn` + `mock.restore()` (restores every spy),
  not `vi`; spy on `console`, which a `process.stdout.write` patch misses.
- A bare `'vitest'` import resolves to `bun:test`, so green `bun test`
  proves no port: grep `from 'vitest'`. One remains
  (`scripts/control-byte-gate/control-byte-gate.test.ts`); swap it when
  touching `scripts/`.
- Stale TSDoc says the suite runs vitest under Node (it is bun):
  `src/utils/claude.ts`, `claude.test.ts`, `src/effort/store.ts`;
  re-derive the rationale, don't swap the word. Editing `store.ts`
  falsifies `store/ndjson.ts`'s note that it is byte-identical to the
  sibling's: fix both, or wait for its (unscheduled) retirement.
- A path-literal sweep of `src/` must not rewrite fixture text:
  `LONG_TASK` is length-pinned (181 chars,
  `src/tests/commit-refusals.test.ts`); `RESIDUE`
  (`src/tests/agents-split-accounting.test.ts`) snapshots the sibling's
  `AGENTS.md`.
- After moving a file with a `new URL('../../x', import.meta.url)` base,
  recount `../` (`src/<subdir>/*.test.ts` reaches the root with `../../`):
  one too many lands silently in parent `open-tomato/` (own `packages/`,
  `AGENTS.md`), where guards pass. Print the resolved path once; guard
  reads with `existsSync` (`findAgentsMaps`, `context-page-imports.test.ts`).
- `.githooks/pre-commit` runs the control-byte gate `--staged`; a manual
  run needs `--include-untracked` to see unadded files.
- `it.skipIf(HAS_BUN)` in `src/utils/claude.test.ts` passes only under
  vitest, kept on purpose (header says why); `collect.test.ts`'s
  `it.skipIf(isRoot)` is an unrelated root guard.
- `bun run lint` exits 1 on 4 pre-existing errors outside `src/`:
  `import/order` (`eslint.base.mjs`), `quotes` + `eol-last` (root
  `index.ts`), `import/no-unresolved` (the vitest import above). A `src/`
  task runs `eslint src/`; a whole-repo-green one fixes all three files.
- `node_modules` can be partial (only `@types`, `bun-types`,
  `typescript`) while `bun.lock` and `package.json` agree: run
  `bun install` and read added vs. satisfied before calling a missing
  module a bug.
- `src/config.ts`: `loadConfig(root, cli, warn?)` ranks command line >
  `.rafa/config.yaml` > `CONFIG_DEFAULTS`; pure `parseConfigText`,
  `resolveConfig`; refusals throw `ConfigError` (`.problems`); unknown
  keys return as `extras` + `warnings`. Command-line values go in RAW by
  setting (`{ store, inject }`, from `argValue`); file `plan.inject` is
  field `inject`.
- The plan gives the `plan.inject` default as `stage` (config sections)
  and as `full` ("Cutover", roadmap risk 1); `CONFIG_DEFAULTS` ships
  `stage`, which the runbook's "one-line default flip" must check.
- `progress.txt` is TRACKED despite `.gitignore` (committed first):
  `git add -A` commits every edit until `git rm --cached progress.txt`;
  `git ls-files` lists it, `git check-ignore` is silent. The root file
  literally named `@progress.txt`, also tracked, holds task summaries;
  findings go here.
- Store port, `src/effort/store/types.ts`: `EffortStore` (`append`,
  `keys`, `read`, per `EffortRowKind`), `EffortRowByKind`,
  `EFFORT_KEY_PROJECTIONS`, `SessionEffortRow` (re-exported by
  `effort/collect.ts`; `SessionMode` `mode` is `'local'` via `SESSION_MODE`
  until phase 6 adds `remote`; `issueIdentifier`, no
  `issueIdentifierSource`); `AppendResult` = `{ path, appended, skipped }`,
  NDJSON-only readings on a backend's wider type.
- Declare ports with PROPERTY signatures (`keys: (kind: K) => ...`): tsc
  refuses a narrower backend parameter (TS2322) a method signature
  accepts bivariantly. Close a per-kind map with a `type` alias, which
  declaration merging cannot widen.
- `check-types` skips `**/*.test.ts` (`tsconfig.json` cites a nonexistent
  `.specs/test-type-checking.md`). To type-check one, use a tsconfig
  OUTSIDE the repo: `extends` `<repo>/tsconfig.json`, `files` [abs path],
  `include` [], absolute `typeRoots` [`<repo>/node_modules/@types`] (else
  `node:fs`/`process` fail); run `./node_modules/.bin/tsc -p <it>`,
  planting an error once. Diff against HEAD:
  `src/effort/attribution.test.ts` has a pre-existing TS2769.
- `src/effort/store/ndjson.ts`: `openNdjsonStore(root)` = port +
  `path(kind)`, `readRows(kind)`, `append` answering `NdjsonAppendResult`;
  it routes to `src/effort/store.ts`, so retiring `store.ts` means MOVING
  its functions in and keeping `ndjson.test.ts`'s parity case (imports
  `openEffortStore`).
- A new `EffortRowByKind` kind breaks `check-types`: TS2345 at each
  `effortStorePath` call in `store/ndjson.ts` until `STORE_FILE_NAMES`
  (`store.ts`) names its file, TS2741 in `store/sqlite.ts` until
  `KIND_TABLES` has it. A SQLite-only table stays outside the port's row
  map.
- A directory at a file's path: `Bun.file(path).exists()` is false and
  `.text()` throws EISDIR, so use `existsSync` where unreadable must not
  read as absent. Planting one tests "unreadable" portably
  (`readFileSync` throws EISDIR even as root: no `it.skipIf(isRoot)`).
- Quote grep globs in the Bash tool: zsh expands an unquoted
  `--include=*.ts`, prints `no matches found` and never runs grep, which
  reads like "no hits".
- `src/effort/store/sqlite.ts`: one file (`.ralph/effort/effort.sqlite`),
  a table per kind, `row_json` = the NDJSON line. Schema changes APPEND to
  `SQLITE_MIGRATIONS` (version = its length, in `PRAGMA user_version`),
  never editing a shipped entry; exported `migrateSchema(db, path,
  migrations?)` runs synthetic histories on `:memory:`.
- `bun:sqlite`, beyond `sqlite.ts`'s header: `new Database(path)`
  CREATES a missing file; `OR IGNORE` swallows CHECK violations too;
  `Statement.run` is sync (`{ changes, lastInsertRowid }`); a trailing
  lone high surrogate binds as `''`; `close()` finalizes `db.query`
  statements at once (any `throwOnError`), `db.prepare` ones only when
  finalized or GC'd, so query-only code closing in `finally` needs no
  `close(true)`.
- Write/Edit decode a four-hex-digit Latin-1 escape into the letter
  (skill `ascii-source-unicode-via-codepoints`), but surrogate (`\uD800`)
  and braced (`\u{e9}`) escapes arrive as typed.
- `src/effort/store/index.ts`: `selectEffortStore(root, config)` takes
  the `config` FIELD of `loadConfig`/`resolveConfig`'s answer, beside
  both openers. Import it from `./store/index.js`, port types from
  `./store/types.js`, never `./store.js` (the sibling's `openEffortStore`,
  `EffortRowKind`, `AppendResult` differ); `index.test.ts` pins exports.
- `import/order` puts all `import type` lines in ONE group ranked by
  segment COUNT (fewer `/` first), not name: `./types.js` before
  `../../config.js`, `../store.js` ties `./types.js`; value imports split
  parent/sibling, so never hit this. `./index.js` is the `index` group
  after a blank line; `./store/index.js` is a plain sibling.
- `src/effort/report.ts` reads `sessions.ndjson` directly
  (`effortStorePath` + `readStoreRows`), not the selected store, and no
  task rewires it: under default `sqlite`, `rafa effort report` sees
  nothing `effort collect` wrote (hence `store: ndjson` in
  `effort-pipeline.test.ts`).
- Drive a command that finds its root via git black-box: spawn
  `[process.execPath, <abs src/rafa.ts>, ...]` in a scratch `git init`
  repo with one `--allow-empty` commit made hook-, gpg- and
  identity-proof (copy `makeRepo`, `src/effort/collect.test.ts`).
- Both backends share ONE `describe.each` file per property
  (`store/refusal.test.ts`, `contract.test.ts`): `StoreWithPath` takes
  both openers uncast, and both throw one keyless-row message (`store.ts`
  `appendRows`, `sqlite.ts` `refused`), so one `toThrow` fits both.
- `sqlite.ts`'s `appendKind` returns early on an EMPTY batch, skipping
  `checkedVersion`/`migrateSchema`: past `SQLITE_MIGRATIONS`,
  `append(kind, [])` answers `{ appended: 0, skipped: 0 }` where every
  other call refuses, and `effort/collect.ts` appends `[]` on each
  "nothing new" run. Finding 1 of `.plans/REVIEW-phase-0-store.md`.
- `resolveIssueIdentifier({ branch, planStub, planIssue? }, teamKeys?)`
  (`src/effort/attribution.ts`) ranks plan `issue:` > plan stub > WHOLE
  branch name and answers null, never a stub; the grammar is team-keyed
  (`ISSUE_TEAM_KEYS` `['OPT']`, any case) as `[a-z]+-\d+` reads `PHASE-0`
  from this branch. `collectSessionRow` copies attribution fields ONE BY
  ONE: a new field reaches no row until copied there.
- Mutation legs (`mutation-leg-grids`): a bun script runs ONE file per
  leg via `Bun.spawnSync([process.execPath, 'test', <file>])`, unmutated
  runs before and after as controls; never run the full suite meanwhile.
- `resolveParityFixture({ env?, home? })` (`src/tests/parity-fixture.ts`)
  never throws: `present: true` (paths, `sessionLogCount`) or
  `present: false` (`absences`, one-line `reason`). Overrides
  `RAFA_PARITY_LOG_DIR`/`RAFA_PARITY_STORE_DIR`; defaults under
  `~/projects/agentic-research` resolve PRESENT here, so gated suites run
  in plain `bun test`. The sibling's git history and `.plans/` are
  derived apart, as `join(homedir(), 'projects', 'agentic-research')`.
- `collectEffort` (`src/effort/collect.ts`) without `store` resolves one
  ONCE per run from `repoRoot`'s config (none: SQLite, both halves'
  `storePath` `effort.sqlite`); pass `openNdjsonStore`/`openSqliteStore`
  to pick (`effort collect` has no `--store`). `repoRoot` also sets
  default `plansDir` and the commit half's git cwd: aimed at the sibling
  it writes the sibling's `.ralph/` unless given a temp-root `store`, or
  use a temp `repoRoot` plus `plansDir` and `readCommits`.
- On bun too (skill `env-gated-suite-skip-readings` measured vitest),
  `describe.skipIf(true)` runs its callback while its hooks and cases
  report `(skip)` unrun: keep fixture reads and heavy setup in
  `beforeAll` or a case.
- `bun:test` hooks/cases take `number | { timeout }` second, overriding
  5000ms PER HOOK. A full `collectEffort` over the live sibling (~1.3GB,
  ~1,053 logs) takes ~4-5s per backend, so a two-backend `beforeAll`
  needs an explicit timeout, else it fails as `(unnamed) [5000.13ms] a
  beforeEach/afterEach hook timed out`, blaming no hook.
- Backends' `read(kind)` rows are `JSON.parse` round-trips: compare
  backends by `JSON.stringify(row)` equality (`toEqual` misses field
  order), paired by key (session id, sha), not position.
- `readStoreRows<Record<string, unknown>>(path)` (`src/effort/store.ts`)
  reads older-schema NDJSON rows as written, checking only for a plain
  object per line; a typed `EffortStore` read would claim absent fields.
- Drop projection keys with an `Object.entries` loop, not
  `const { mode: _mode, ...rest } = row`:
  `@typescript-eslint/no-unused-vars` has no ignore pattern or
  `ignoreRestSiblings` here, so `_` discards are flagged.
- `src/plan/blocks.ts`: `readRafaBlocks(md)` answers every `rafa:*`
  fence as `{ kind, body, span, closed }`, unknown kinds kept
  (`RAFA_BLOCK_KINDS`/`isRafaBlockKind` name this phase's four). `span`
  is 1-based inclusive, fences included; `TaskInfo.lineNum` is 0-based,
  so compare as `lineNum + 1`.
- Illustrate the plan format inside a LONGER outer fence (4 backticks):
  in a 3-backtick one the first inner closing fence closes the OUTER, and
  later `rafa:` fences become real blocks to any CommonMark reader.
- CommonMark oracle: `mdast-util-from-markdown` (micromark; transitive
  via `@eslint/markdown`, so tests don't import it) loads into a scratch
  script by absolute path to its `node_modules/.../index.js`.
- `bunx eslint --stdin --stdin-filename <path>` over a planted violation
  proves a path is linted at all, file untouched: the control a
  zero-exit `eslint <dir>` needs.
- `src/plan/parse.ts`: `parsePlan(md)` never throws: `{ header, context,
  stages, tasks, blocks, issues }`, `tasks` flat, each with a `stage`
  index (null above the first heading), `status`
  `unchecked`/`blocked`/`done`, `task` (= `TaskInfo.task`),
  `text`/`declaration` (= `parseTaskDeclaration`) and 0-based `lineNum`;
  `PlanIssue.line` is 1-based.
- The block reader tracks EVERY fence by CommonMark (a `rafa:` fence
  inside another is body). `findNextTask` skips task lines inside CLOSED
  `rafa:*` blocks (via `readRafaBlocks`) and is fence-blind elsewhere
  (dispatches a `- [ ] ` in any other fence); `parsePlan` reads `rafa:*`
  spans as body, so the two agree except after an UNCLOSED `rafa:`
  fence, which `findNextTask` ignores on purpose (skipping would answer
  null, which `start.ts` wraps up as a finished plan): the model reports
  `task-in-block` and the dispatched `lineNum` matches nothing in
  `model.tasks`. The 27 local plans (this repo's 2, the sibling's 25)
  hold 0 `rafa:*` blocks, so a real-plan test over them exercises the
  checklist only.
- The `rafa:*` strip rule lives at the SOURCE, `findNextTask`: all four
  quoting sites (prompt header and log in `dispatchTask`, log, subject
  and body in `commitFinishedTask`) quote `taskInfo.task`, so a caller
  building its own `TaskInfo` (a Tracker port) bypasses it. A stray
  bare fence closing a `rafa:` block late swallows real tasks; the one
  signal is `announcePlanIssues` in `start.ts`, which prints
  `parsePlan`'s issues once at start.
- A commit BODY is empty unless the subject truncated (72 chars with the
  `type: ` prefix), so an assertion that something is absent from the
  body needs a planted line longer than that, or it passes vacuously.
- `parseTaskDeclaration` needs one `DECLARATION_KEYS` key, so
  `Task  {skills=x}` is no declaration: the block stays in the text and
  resolves null. Beside a known key, `skills` lands in `extras`, stripped.
  The spec calls `skills=` "a new recognised key".
- `Bun.YAML.parse` traps are in the headers of `src/config.ts` ("The
  parser, measured on bun 1.3.14", the `Map`/`Set` lookup rule) and
  `src/plan/parse.ts` (`issue: #42` is `null`, `42`/`042` the number 42,
  integer-like keys enumerate first, `true` a boolean). Quote such values.
- `src/plan/index.ts` is the `./plan` barrel; `index.test.ts` pins its
  runtime names (`RUNTIME_EXPORTS`), identity, and that from `src/` both
  `./plan` and `./plan.js` resolve to `src/plan.ts` (the `rafa plan`
  command), never the barrel, so in-package code imports
  `./plan/index.js` (bun 1.3.14). A new `src/report/parse.ts` must join
  the barrel and that list, and rewrite its "not exported" TSDoc section.
  `package.json` has NO `main`, `exports`, `bin` or `files` yet.
- `CLAUDECODE` (set in Claude Code sessions) makes `bun test` print
  counts only (no `(pass)`/`(skip)` lines, skip reasons, file names), so
  grepping a full-suite capture for a file finds nothing;
  `env -u CLAUDECODE` restores them (unsetting `CLAUDE_CODE_ENTRYPOINT`
  does not), and `--reporter=junit --reporter-outfile=<f>` lists skips
  anyway with `file="..."` per suite and case. Count DISTINCT failing
  titles: the summary re-lists `(fail)`.
- `.claude/skills/dev-planner/SKILL.md` line 153 still names
  `tools/ralph/utils/tracker.ts`; the dev-planner rewrite task owns it.
- `src/plan/inject.ts`: `renderInjection({ mode, plan, task })` (`task`:
  `TaskInfo`'s `task` + `lineNum`) never throws: `{ requested, mode,
  text, fallback }`. `full` = the plan's bytes; `stage`/`task` build from
  `parsePlan` (`## Plan context`, `## Stage: <name>` or `## Checklist`
  above every heading, `## Stages`; task lines checkbox + `text`,
  declaration off), identical for plan and tracker. A task not held at
  `lineNum` (plan edited after the tracker copy, or after an unclosed
  `rafa:` fence) falls back to `full`, `fallback.reason`
  `no-task-at-line` or `task-text-differs`, which `dispatchTask` warns.
- Injection sizes, 2026-09-13 (14 local plans, 1,051 dispatches, 0
  fallbacks): phase-0's 62 total 1,740,588 bytes `full`, 162,672
  `stage`, 16,637 `task`.
- `src/start.ts` injection wiring: `loadRunConfig(root, args, warn?)`
  ranks `--inject=` over `.rafa/config.yaml` (a bare `--inject` is
  refused as `''`), resolved BEFORE `--start-at` defers; `dispatchTask`
  REQUIRES `inject` (no second default beside `CONFIG_DEFAULTS`) and
  records `injection`; `buildWrapUpPrompt(branch, planContent)` appends
  the whole plan below its classifier-key line and takes no mode.
  Integration tests: `src/tests/plan-injection.test.ts`.
- `planStubFromPrompt` answers the FIRST `<!-- ralph:plan=... -->` in
  a prompt, and task and wrap-up prompts embed plan text ABOVE the
  appended stamp, so a plan quoting a marker misattributes its
  sessions. No `PLAN*.md` in either `.plans/` carries one today.
- eslint ignores `.claude/skills/*/SKILL.md` (measured: "File ignored
  because of a matching ignore pattern", exit 0), so skill prose has no
  lint gate.
- A task to "add negative parser tests covering X, Y, Z" usually finds
  most named cases already pinned in the module's colocated test file
  (`parse.test.ts` held 4 of `parse.ts`'s 5): grep the suite for each
  named case first and add only the gap.
- Pin a real-plan-file test to `.plans/PLAN-*.md`, the frozen template:
  the loop ticks `PLAN_TRACKER-*.md` `[ ]` to `[x]` on dispatch, so it
  drifts task by task under a fixed stage/task/declaration expectation.
- `bunx eslint --fix` traps: a nested `a < b ? -1 : a > b ? 1 : 0`
  comparator is reflowed onto four lines by `@stylistic/multiline-ternary`,
  not flagged, so sort strings with `a.localeCompare(b)`; an apostrophe
  in a single-quoted `it(...)`/`describe(...)` title is backslash-escaped
  (`\'s`), not requoted (double quotes are forbidden), so write titles
  without one.
- A "strip-proof" test over a `rafa:*` block needs the block's body
  written AS a checklist line (`- [ ] <marker>`), not as plain prose: a
  prose-only block body can never be mistaken for a task regardless of
  whether `findNextTask`'s exclusion still works, so every downstream
  absence assertion (prompt head, operator log, commit subject/body)
  passes whether or not the strip is doing anything. Confirmed by
  mutating `findNextTask` to drop its `inBlock.has(i)` skip: only the
  checklist-shaped marker reddens (`src/tests/rafa-block-strip.test.ts`).
- `renderInjection({ mode: 'full', ... })` (`src/plan/inject.ts`) never
  falls back — `fallback` is always null under `full`, which locates no
  task by line at all — so a dispatch test driven under `full` asserts
  zero warnings without a positive control for that absence.
