- The plan's "Structured task report" stage deletes
  `.claude/skills/progress-hygiene`; docs must not treat it as permanent.
- The sandbox blocks `rm -r`/`rm -rf` on any path, /tmp included: clear a
  directory by `mv`-ing its contents to a scratch one; copy contents into
  an existing directory with `cp -R src/. dest/`.
- Relative imports under `src/` use `.js` specifiers for `.ts` files;
  keep them when moving files.
- Tests use `bun:test`'s `spyOn` and `mock.restore()` (restores every
  spy), not the `vi` alias; spy on `console`, which a
  `process.stdout.write` patch misses.
- A bare `'vitest'` import resolves to `bun:test`, so a green `bun test`
  does not prove a port done: grep `from 'vitest'`. One remains, in
  `scripts/control-byte-gate/control-byte-gate.test.ts`; swap it when
  touching `scripts/`.
- `tsconfig.json` excludes `**/*.test.ts` from `bun run check-types`
  (citing a nonexistent `.specs/test-type-checking.md`), so an editor
  error in a test file (e.g. `toEqual()` on a `readonly T[]`) is no
  build break until that command says so.
- Stale TSDoc says the suite runs vitest under Node (it is bun):
  `src/utils/claude.ts`, `claude.test.ts`, `src/effort/store.ts`;
  re-derive the rationale, don't swap the word. Editing `store.ts`
  falsifies `store/ndjson.ts`'s note that it is byte-identical to the
  sibling's `tools/ralph/effort/store.ts`: fix both, or wait for
  `store.ts`'s retirement (no task schedules it).
- Repoint only real `tools/ralph` path literals in `src/` (TSDoc refs,
  `new URL`/`readFileSync` targets, CLI usage), never fixture text:
  `LONG_TASK` is length-pinned (181 chars,
  `src/tests/commit-refusals.test.ts`), and `RESIDUE`
  (`src/tests/agents-split-accounting.test.ts`) snapshots the sibling's
  `AGENTS.md`.
- After moving a file with a `new URL('../../foo', import.meta.url)`
  base, recount `../` (`src/<subdir>/*.test.ts` reaches the root with
  `../../`): one too many lands silently in the parent `open-tomato/`
  (own `packages/`, `AGENTS.md`), where guards pass. Print the resolved
  path once; guard reads with `existsSync` (`findAgentsMaps`,
  `src/tests/context-page-imports.test.ts`).
- `.githooks/pre-commit` runs the control-byte gate `--staged`, so new
  files are first checked at commit; a manual run of
  `scripts/control-byte-gate/control-byte-gate.ts` needs
  `--include-untracked` for unadded files.
- `it.skipIf(HAS_BUN)` in `src/utils/claude.test.ts` passes only under
  vitest and is kept on purpose (header says why); `collect.test.ts`'s
  `it.skipIf(isRoot)` is an unrelated root guard.
- `bun run lint` exits 1 on 4 pre-existing errors outside `src/`
  (`import/order` in `eslint.base.mjs`; `quotes`, `eol-last` in root
  `index.ts`; `import/no-unresolved` on the vitest import above). A
  `src/` task runs `eslint src/`; a whole-repo-green one fixes all three
  files.
- `node_modules` can be partial (e.g. only `@types`, `bun-types`,
  `typescript`) while `bun.lock` and `package.json` agree: run
  `bun install` and read added vs. satisfied before calling a missing
  module a real bug.
- `Bun.YAML.parse` traps: `src/config.ts`'s header ("The parser,
  measured on bun 1.3.14", the `Map`/`Set` lookup rule). Missing there:
  integer-like keys enumerate first, so `Object.entries` is not source
  order.
- Config, `src/config.ts`: `loadConfig(root, cli, warn?)` ranks command
  line > `.rafa/config.yaml` > `CONFIG_DEFAULTS`; `parseConfigText` and
  `resolveConfig` are pure; refusals throw `ConfigError` (`.problems`);
  unknown keys come back as `extras` + `warnings`. Pass command-line
  values RAW keyed by setting (`{ store, inject }`, from `argValue`);
  file key `plan.inject` is field `inject`.
- The plan says `stage` for the `plan.inject` default ("Config surface",
  the spec's table, the config task) and `full` ("Cutover", roadmap risk
  1). `src/config.ts` ships `stage` in `CONFIG_DEFAULTS`; the runbook's
  "one-line default flip" must check that line.
- `progress.txt` is TRACKED despite `.gitignore` (committed before the
  rule), so `git add -A` commits every edit until
  `git rm --cached progress.txt` runs; `git ls-files` shows it,
  `git check-ignore` is silent. `@progress.txt` (tracked) holds task
  summaries; findings go here.
- Store port, `src/effort/store/types.ts`: `EffortStore` (`append`,
  `keys`, `read`, per `EffortRowKind`), `EffortRowByKind`,
  `EFFORT_KEY_PROJECTIONS`, `SessionEffortRow` (re-exported by
  `effort/collect.ts`). `AppendResult` is `{ path, appended, skipped }`;
  NDJSON-only readings go on a backend's wider type.
- Declare ports with PROPERTY signatures (`keys: (kind: K) => ...`):
  tsc refuses a narrower backend parameter (TS2322) that a method
  signature accepts bivariantly. Close a per-kind map with a `type`
  alias, which declaration merging cannot widen.
- Type-check one `*.test.ts` via a tsconfig OUTSIDE the repo:
  `"extends": "<repo>/tsconfig.json"`, `"files": ["<abs path>"]`,
  `"include": []`, absolute `"typeRoots": ["<repo>/node_modules/@types"]`
  (else `node:fs`/`process` fail); run `./node_modules/.bin/tsc -p <it>`, planting an error once to prove
  it live. `src/effort/attribution.test.ts` has a pre-existing TS2769
  (`toEqual(MEASURED_BRANCHES)`): diff errors against HEAD.
- NDJSON backend, `src/effort/store/ndjson.ts`:
  `openNdjsonStore(repoRoot)` = port + `path(kind)`, `readRows(kind)`,
  `append` answering `NdjsonAppendResult`. It routes to
  `src/effort/store.ts`, so retiring `store.ts` means MOVING its
  functions in and carrying over `ndjson.test.ts`'s parity case (imports
  `openEffortStore`).
- A new `EffortRowByKind` kind (e.g. `findings`) breaks `check-types`:
  TS2345 at each `effortStorePath` call in `store/ndjson.ts` until
  `STORE_FILE_NAMES` (`src/effort/store.ts`) names its file, TS2741 in
  `store/sqlite.ts` until `KIND_TABLES` has it. A SQLite-only table
  stays outside the port's row map.
- A directory at a file's path: `Bun.file(path).exists()` is false and
  `.text()` throws EISDIR, so use `existsSync` where unreadable must not
  read as absent. Planting one tests "unreadable" portably:
  `readFileSync` throws EISDIR even as root, so no `it.skipIf(isRoot)`.
- Quote grep globs in the Bash tool: zsh expands an unquoted
  `--include=*.ts`, prints `no matches found` and never runs grep, which
  reads like "no hits".
- SQLite backend, `src/effort/store/sqlite.ts`: one file
  (`.ralph/effort/effort.sqlite`), a table per kind, `row_json` holding
  the NDJSON line text. Schema changes APPEND to `SQLITE_MIGRATIONS`
  (version = its length, in `PRAGMA user_version`), never edit a shipped
  entry; `migrateSchema(db, path, migrations?)` is exported for
  synthetic histories on `:memory:`.
- `bun:sqlite` facts are in `sqlite.ts`'s header. Missing there:
  `new Database(path)` CREATES a missing file; `OR IGNORE` also
  swallows CHECK violations; `Statement.run` is sync, returning
  `{ changes, lastInsertRowid }`; a trailing lone high surrogate binds
  as `''`; `close()` finalizes `db.query` statements at once (whatever
  `throwOnError`) while `db.prepare` ones linger until finalized or
  GC'd, so query-only code closing in a `finally` needs no `close(true)`
  (as `sqlite.ts` does).
- Write/Edit decode a plain four-hex-digit escape of a Latin-1 letter
  into the letter (Edits then "make no change"); surrogate (`\uD800`)
  and braced (`\u{e9}`) escapes arrive as typed. Check what landed by
  listing the file's non-ASCII code points with bun.
- Store entry, `src/effort/store/index.ts`: `selectEffortStore(root,
  config)` takes the `config` FIELD of `loadConfig`/`resolveConfig`'s
  answer, beside both openers. Import it from `./store/index.js` and
  port types from `./store/types.js`, never `./store.js` (the sibling's
  module: its `openEffortStore`, `EffortRowKind`, `AppendResult`
  differ). `index.test.ts` pins the export list.
- `import/order` ranks all `import type` lines in ONE group by segment
  COUNT (fewer `/` first), not name: `./types.js` before
  `../../config.js`; `../store.js` ties `./types.js`. Value imports
  split parent/sibling, so never hit this. `./index.js` is the `index`
  group after a blank line; `./store/index.js` from above is a plain
  sibling (`effort/collect.ts`).
- `src/effort/report.ts` reads `sessions.ndjson` directly
  (`effortStorePath` + `readStoreRows`), not the selected store, and no
  task rewires it: under the default config (`sqlite`), `rafa effort
  report` sees nothing `effort collect` wrote; hence `store: ndjson` in
  `src/tests/effort-pipeline.test.ts`.
- A command resolving its root via git from the cwd is driven
  black-box: spawn `[process.execPath, <abs src/rafa.ts>, ...]` in a
  scratch `git init` repo with one `--allow-empty --no-verify` commit
  made under `-c user.name=... -c user.email=... -c commit.gpgsign=false
  -c core.hooksPath=/dev/null` (`makeRepo`/`runCollect`,
  `src/effort/collect.test.ts`).
- Both backends share ONE `describe.each` file per property
  (`src/effort/store/refusal.test.ts`, `contract.test.ts`); their
  `StoreWithPath` takes both openers uncast, and both throw the identical
  keyless-row message (`store.ts`'s `appendRows`, `sqlite.ts`'s
  `refused`), so one `toThrow` holds both.
- `sqlite.ts`'s `appendKind` returns early on an EMPTY batch, skipping
  `checkedVersion`/`migrateSchema`: `append(kind, [])` on a store past
  `SQLITE_MIGRATIONS` answers `{ appended: 0, skipped: 0 }` where every
  other call refuses, and `effort/collect.ts` appends `[]` on each
  "nothing new" run, printing `+0 rows`. Finding 1 of
  `.plans/REVIEW-phase-0-store.md`.
- `resolveIssueIdentifier({ branch, planStub, planIssue? }, teamKeys?)`
  (`src/effort/attribution.ts`) ranks plan `issue:` > plan stub > WHOLE
  branch name, and answers null, never a stub. Its grammar is team-keyed
  (`ISSUE_TEAM_KEYS`, `['OPT']`, case-insensitive); a generic
  `[a-z]+-\d+` reads `PHASE-0` from this branch. `collectSessionRow`
  (`effort/collect.ts`) copies attribution fields ONE BY ONE: a new
  field reaches no row until copied there.
- Driving mutation legs (`mutation-leg-grids` skill): a bun script runs
  ONE file per leg via `Bun.spawnSync([process.execPath, 'test',
  <file>])`, the unmutated file before and after as controls; never run
  the full suite meanwhile, as the module on disk is mutated.
- Proving a `stamped` plan-stub match (`resolveSessionPlan`'s stamp
  outranks the branch) needs a branch resolving on its own to a
  DIFFERENT real roster stub (stamping a `none` one only proves the stamp
  is read): `collect.test.ts` plants a second `PLAN-*.md` and stamps it.
- `CLAUDECODE` (set in Claude Code sessions) makes `bun test` print
  counts only, no `(pass)`/`(skip)` lines or skip reasons.
  `env -u CLAUDECODE bun test <file>` restores them (unsetting
  `CLAUDE_CODE_ENTRYPOINT` does not); `--reporter=junit
  --reporter-outfile=<f>` lists skips either way. Count DISTINCT failing
  titles: the summary re-lists `(fail)` lines.
- `resolveParityFixture({ env?, home? })` (`src/tests/parity-fixture.ts`)
  never throws: `present: true` (`logDir`, `storeDir`, `sessionsPath`,
  `commitsPath`, `sessionLogCount`) or `present: false` (`absences`, a
  one-line `reason`). Overrides `RAFA_PARITY_LOG_DIR`,
  `RAFA_PARITY_STORE_DIR`; defaults under `~/projects/agentic-research`
  resolve PRESENT here, so gated suites run in plain `bun test`. The
  sibling's git history and `.plans/` are derived separately, as
  `join(homedir(), 'projects', 'agentic-research')`.
- `collectEffort` (`src/effort/collect.ts`): with no `store` it resolves
  one ONCE per run from config under `repoRoot` (no config: SQLite, both
  halves' `storePath` = `effort.sqlite`); pass `openNdjsonStore(root)`
  or `openSqliteStore(root)` to pick one (`effort collect` has no
  `--store`). `repoRoot` also sets the default `plansDir` and the commit
  half's git cwd, so aimed at the sibling it writes the sibling's
  `.ralph/` unless a temp-root `store` is passed; or keep `repoRoot`
  temp and pass `plansDir` and `readCommits`.
- On bun too (skills `env-gated-suite-skip-readings`,
  `port-parity-differential-testing` measured vitest),
  `describe.skipIf(true)` runs its callback while hooks and cases inside
  report `(skip)` unrun: keep fixture reads and heavy setup in
  `beforeAll` or a case.
- `bun:test` hooks/cases take `number | { timeout }` as a second
  argument, overriding 5000ms PER HOOK. A full `collectEffort` over the
  live sibling (~1,053 logs, ~1.3GB, ~1,214 commits) takes ~4-5s per
  backend, so a two-backend `beforeAll` needs one explicit timeout, else
  it fails as `(unnamed) [5000.13ms] a beforeEach/afterEach hook timed
  out`, blaming no hook.
- Backends' `read(kind)` rows are `JSON.parse` round-trips, so the
  strongest cross-backend check is `JSON.stringify(row)` equality
  (`toEqual` misses field order), paired by key (session id, sha), not
  position.
- The sibling's `.plans/` roster spreads sessions over 12 plan-stub
  buckets (`(none)` 105 of ~1,053): non-vacuity evidence for per-stub
  groupings of the live fixture.
- `src/tests/parity-lineage.test.ts` collects ONCE (NDJSON) over the
  live sibling and compares with the sibling's OWN stored rows (via
  `readStoreRows`). 3 of 1,052 stored session rows are frozen
  mid-session (the log grew afterwards, per `effort/collect.ts`'s module
  note), so every content field differs: accept them only by
  `sizeBytes`/`modifiedAt` having grown, and fail a row that shrank.
- `readStoreRows<Record<string, unknown>>(path)` (`src/effort/store.ts`)
  reads older-schema NDJSON rows as written, checking only for a plain
  object per line; a typed `EffortStore` read would claim absent fields.
- Drop projection keys with an `Object.entries` loop, not
  `const { mode: _mode, ...rest } = row`: `@typescript-eslint/no-unused-vars`
  has no ignore pattern or `ignoreRestSiblings` here, so `_` discards
  are flagged.
- `SessionEffortRow` carries `mode` (`SessionMode`, `'local'` until phase
  6 adds `remote`; from `SESSION_MODE`) and `issueIdentifier`, not
  `issueIdentifierSource`. The sibling's stored rows predate both, and
  74 of 1,052 end in a `planStubPatch` no collector here writes; drop
  the two and a fresh row has the stored key order, so project to the
  sibling's keys, handle `planStubPatch` explicitly, compare by
  `JSON.stringify`. Commit rows match byte-for-byte unprojected.
- Block reader, `src/plan/blocks.ts`: `readRafaBlocks(md)` answers every
  `rafa:*` fence as `{ kind, body, span, closed }`, unknown kinds kept
  (`RAFA_BLOCK_KINDS`/`isRafaBlockKind` name this phase's four). `span`
  is 1-based inclusive, fences included (`sed -n 'first,lastp'`), while
  `TaskInfo.lineNum` is 0-based: compare as `lineNum + 1`.
- The block reader tracks EVERY fence by CommonMark rules, so a `rafa:`
  fence inside another fence is body; `findNextTask` tracks none
  (measured: a `- [ ] ` inside a fence is dispatched), so a block-aware
  task list has to exclude the lines inside a span itself.
- Illustrating the plan format needs a LONGER outer fence (4 backticks):
  inside a 3-backtick outer, the first inner closing fence closes the
  OUTER one and later `rafa:` fences are real blocks to any CommonMark
  reader. `.specs/phase-0-package-parity-cutover.md` reads 2 such blocks
  (lines 149, 156); the phase-0 PLAN, fenced with 4, reads 0.
- CommonMark oracle: `mdast-util-from-markdown` (micromark, installed via
  `@eslint/markdown`) imports into a scratch script by absolute path,
  `<repo>/node_modules/mdast-util-from-markdown/index.js`. It is a
  transitive dependency, so tests do not import it.
- `bunx eslint --stdin --stdin-filename <path>` over a planted violation
  proves a path is linted at all, without touching the file: the control
  a zero-exit `eslint <dir>` needs.
